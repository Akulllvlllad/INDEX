stages:
  - lint

eslint: 
  stage: lint
  variables:
    NODE_VERSION: 20
  script:
    - curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
    - npm ci
    - npm eun lint